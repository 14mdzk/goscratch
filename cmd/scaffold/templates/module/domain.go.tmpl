package domain

import "time"

// {{ .Title }} represents the {{ .Name }} entity.
//
// TODO(scaffold): add fields that belong to the {{ .Name }} domain model.
type {{ .Title }} struct {
	ID        string    `json:"id"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
