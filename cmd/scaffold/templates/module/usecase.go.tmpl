package usecase

import "context"

// {{ .Name }}UseCase implements the UseCase interface for the {{ .Name }} module.
//
// TODO(scaffold): inject repository and any platform dependencies via NewUseCase.
type {{ .Name }}UseCase struct{}

// NewUseCase creates a new {{ .Name }} use case.
//
// TODO(scaffold): add repository / transactor / cache parameters as needed and
// wire this constructor in internal/platform/app/app.go.
func NewUseCase() UseCase {
	return &{{ .Name }}UseCase{}
}

// GetByID retrieves a {{ .Name }} record by ID.
//
// TODO(scaffold): implement real lookup via the injected repository.
func (uc *{{ .Name }}UseCase) GetByID(_ context.Context, id string) (*{{ .Title }}, error) {
	return &{{ .Title }}{ID: id}, nil
}
