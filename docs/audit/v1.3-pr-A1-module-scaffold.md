# v1.3 PR A1 — Module Scaffold Generator

**Branch:** `feat/module-scaffold`
**Roadmap row:** v1.3 Tier A, A1
**ADR:** `docs/adr/008-module-scaffold.md`

---

## Files Touched

### New

| Path | Role |
|------|------|
| `cmd/scaffold/main.go` | CLI entry point; subcommand dispatch via stdlib `flag` |
| `cmd/scaffold/scaffold.go` | Core logic: validation, template rendering, file generation |
| `cmd/scaffold/scaffold_test.go` | Unit + integration tests for scaffold logic |
| `cmd/scaffold/templates/module/domain.go.tmpl` | Entity struct template |
| `cmd/scaffold/templates/module/port.go.tmpl` | `UseCase` interface template |
| `cmd/scaffold/templates/module/usecase.go.tmpl` | Concrete usecase template |
| `cmd/scaffold/templates/module/handler.go.tmpl` | Fiber handler template |
| `cmd/scaffold/templates/module/handler_test.go.tmpl` | Handler smoke test template |
| `cmd/scaffold/templates/module/usecase_test.go.tmpl` | Usecase smoke test template |
| `docs/adr/008-module-scaffold.md` | Architecture decision record |
| `docs/audit/v1.3-pr-A1-module-scaffold.md` | This file |

### Edited

| Path | Change |
|------|--------|
| `Makefile` | Appended `new-module` target and added to `.PHONY` list |
| `CHANGELOG.md` | Added bullet under `[Unreleased]` → `### Added` |

---

## Explicitly Out of Scope

- `internal/platform/app/app.go` — no auto-wiring. Generated files emit `TODO(scaffold)` comments instead.
- Any auth, authz, JWT, or token code.
- Any migration files under `migrations/`.
- Any existing module files under `internal/module/user/`, `internal/module/auth/`, etc.
- A `--force` flag for overwriting existing modules.
- The `migration` subcommand (wave 2, row A2).
- OpenAPI spec updates.
- Any worker or lifecycle code.

---

## Acceptance Checklist

- [ ] `make lint test` exits 0 from the worktree root.
- [ ] `make new-module name=demoscaffold` produces a directory that compiles and `go test ./internal/module/demoscaffold/...` is green; demo directory removed before final commit.
- [ ] `cmd/scaffold/scaffold_test.go` covers name validation (valid + invalid inputs), idempotency refusal, and template rendering against `t.TempDir()`.
- [ ] `CHANGELOG.md` `[Unreleased]` → `### Added` entry present and matches existing prose style.
- [ ] `docs/adr/008-module-scaffold.md` exists and documents the canonical layout, embed strategy, and `TODO(scaffold)` wiring convention.
