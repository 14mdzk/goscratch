# v1.3 PR C1 — Spatial Primitive Value Types

Roadmap reference: `docs/ROADMAP.md` Tier C, row C1.

## Files Touched

| Path | Change |
|------|--------|
| `internal/shared/domain/geo/doc.go` | New — package doc comment and rationale |
| `internal/shared/domain/geo/errors.go` | New — sentinel error variables |
| `internal/shared/domain/geo/point.go` | New — `Point` value type |
| `internal/shared/domain/geo/bbox.go` | New — `BoundingBox` value type |
| `internal/shared/domain/geo/polygon.go` | New — `Polygon` value type |
| `internal/shared/domain/geo/distance.go` | New — `Distance` value type |
| `internal/shared/domain/geo/point_test.go` | New — table-driven unit tests |
| `internal/shared/domain/geo/bbox_test.go` | New — table-driven unit tests |
| `internal/shared/domain/geo/polygon_test.go` | New — table-driven unit tests |
| `internal/shared/domain/geo/distance_test.go` | New — table-driven unit tests |
| `docs/audit/v1.3-pr-C1-geo-primitives.md` | New — this scope file |
| `CHANGELOG.md` | Edited — one bullet under `[Unreleased] ### Added` |

## Explicitly Out of Scope

- **pgx `Scan` / `Value` round-trip** (WKB encode/decode) — wave 2, row C2. Depends on F2 (PostGIS testcontainer image bump in `internal/platform/testutil/containers.go`).
- **GeoJSON `MarshalJSON` / `UnmarshalJSON`** — wave 2, row C3.
- **Spatial computation helpers** (`Haversine`, `ST_DWithin`, point-in-polygon) — wave 2, row C4 (`pkg/geoutil`).
- **Any concrete domain module** (`internal/module/location`, `internal/module/points_of_interest`, etc.) — the first consumer defines its own table; this PR ships types only.
- Any file under `internal/module/`, `internal/adapter/`, `migrations/`, `Makefile`, `docker-compose.yml`, or auth/lifecycle/schema paths.
- Third-party geo libraries (`go-geom`, `orb`, etc.).

## Acceptance Checklist

- [x] `make lint` exits 0 (no lint errors).
- [x] `make test` exits 0 (all tests pass, no regressions).
- [x] `go test -cover ./internal/shared/domain/geo/...` reports ≥ 95% coverage (actual: 100%).
- [x] `CHANGELOG.md` `[Unreleased]` bullet added under `### Added` matching existing entry style.
- [x] This scope file exists with files-touched list, out-of-scope list, and this checklist.
